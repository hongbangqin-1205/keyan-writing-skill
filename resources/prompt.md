# Agent 写作提示

对每个目标节点执行以下顺序：

1. 读取目标节点的完整祖先路径和对应小节规范。
2. 检查同名来源并进行归属仲裁；同名但语义不属于本节时不得复制。
3. 检查相关标题，比较标题、祖先路径、正文和规范，不按编号硬匹配。
4. 检查建设方案全文正文和表格；范围内弱命中不能阻断全文更优候选。
5. 再检查用户补充材料，最后才查知识库和授权网络来源。
6. 复用原文时保留数字、金额、单位、主体和条件；多段只允许排序和添加独立过渡句。
7. 表格必须整体迁移，保留表头、行、列、合并关系、金额和备注。
8. 证据不足时写明 GAP 或待核事项，不使用经验值冒充项目事实。
