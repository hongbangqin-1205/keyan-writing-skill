# 示例：生成指定小节

用户请求：

> 根据我上传的建设方案，按技能内置可研模板生成第五章第九节“信息资源共享”。

技能执行要点：

1. 读取已扫描的 source，并使用 `init` 自动生成的内置 template 目录树。
2. 先做同名标题归属仲裁，再做相关标题和正文检索。
3. 保留来源段落、表格和事实；按第五章第九节规范补齐结构。
4. 输出 `plan/`、`drafts/`、`chapters/` 和 `reports/` 中的结果。
