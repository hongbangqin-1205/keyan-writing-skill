# legacy（旧版文档，仅存档）

这里的文件来自本技能的**早期版本**，描述的是另一套已被替换的 CLI：

- `authoring-workflow.md` / `cli-reference.md` / `chapter-05-prompt-pack.md` /
  `context-check-matrix.md` / `chapter-methodology.md` 中出现的命令
  （`template`、`validate-outline`、`validate-evidence`、`validate-dossier`、`validate-plan`、
  `check-depth`、`check-provenance`、`check-duplication`、`extract-features`、`check-features`、
  `check-baseline`、`self-check`、`register-chapter`、`check-chapter`、`check-consistency`、
  `confirm_facts`、`confirm_outline` 等）在当前版本中**都不存在**，执行会直接报错。
- `source-priority.md`、`quality-bar.md`、`chapter-methodology.md` 中的**方法论部分**
  （来源优先级、事实基线、体量与深度契约、单章生成规则）仍然有价值，可作为写作思路参考。

当前流程与命令请看：

- `../../SKILL.md` —— 技能入口、命令表、铁律
- `../workflow.md` —— 全流程细则（与本版实现一致）
- `../chapter-writing/` —— 章节规范，写作质量基准

存档原因：保留方法论表述，避免误当成现行接口。**不要照抄本目录里的命令。**